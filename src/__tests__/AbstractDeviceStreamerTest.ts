import AbstractBiosensorsTest from './AbstractBiosensorsTest'

export default class AbstractDeviceStreamerTest extends AbstractBiosensorsTest {
    protected static async beforeEach() {
        await super.beforeEach()
    }
}
