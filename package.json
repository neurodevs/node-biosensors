{
  "name": "@neurodevs/node-biosensors",
  "version": "5.2.10",
  "description": "A Node.js package for various biosensors and associated biosignals.",
  "keywords": [
    "nodejs",
    "typescript",
    "tdd",
    "biosensors",
    "biosignals",
    "real-time",
    "neurotech",
    "bci",
    "lsl",
    "xdf",
    "ble",
    "eeg",
    "ecg",
    "ppg"
  ],
  "license": "MIT",
  "author": "Eric Yates <hello@ericthecurious.com>",
  "homepage": "https://github.com/neurodevs/node-biosensors",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neurodevs/node-biosensors.git"
  },
  "bugs": {
    "url": "https://github.com/neurodevs/node-biosensors/issues"
  },
  "main": "build/index.js",
  "scripts": {
    "build.ci": "yarn run build.tsc && yarn run build.resolve-paths && yarn run lint",
    "build.dev": "yarn run build.tsc --sourceMap ; yarn run resolve-paths.lint",
    "build.copy-files": "mkdir -p build && rsync -avzq --exclude='*.ts' ./src/ ./build/",
    "build.resolve-paths": "resolve-path-aliases --target build --patterns '**/*.js,**/*.d.ts'",
    "build.tsc": "yarn run build.copy-files && tsc",
    "clean": "yarn run clean.build",
    "clean.all": "yarn run clean.dependencies && yarn run clean.build",
    "clean.build": "rm -rf build/",
    "clean.dependencies": "rm -rf node_modules/ package-lock.json yarn.lock",
    "fix.lint": "eslint --fix --cache '**/*.ts'",
    "lint": "eslint --cache '**/*.ts'",
    "lint.tsc": "tsc -p . --noEmit",
    "post.watch.build": "yarn run build.copy-files && yarn run build.resolve-paths",
    "rebuild": "yarn run clean.all && yarn install && yarn run build.dev",
    "update.dependencies": "yarn run clean.dependencies && yarn",
    "resolve-paths.lint": "yarn run build.resolve-paths ; yarn run lint",
    "test": "jest",
    "watch.build.dev": "tsc-watch --sourceMap --onCompilationComplete 'yarn run post.watch.build'",
    "watch.rebuild": "yarn run clean.all && yarn install && yarn run watch.build.dev",
    "watch.tsc": "tsc -w"
  },
  "dependencies": {
    "@neurodevs/node-ble": "^1.0.1",
    "@neurodevs/node-lsl": "^11.4.2",
    "@neurodevs/node-xdf": "^1.4.5",
    "ftdi-d2xx": "^1.3.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@neurodevs/generate-id": "^1.0.0",
    "@sprucelabs/jest-json-reporter": "^8.0.490",
    "@sprucelabs/resolve-path-aliases": "^2.0.527",
    "@sprucelabs/test": "^9.0.53",
    "@sprucelabs/test-utils": "^5.1.443",
    "@testing-library/dom": "^10.4.1",
    "@testing-library/react": "^16.3.0",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.7.7",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "chokidar-cli": "^3.0.0",
    "eslint": "^9.13.0",
    "eslint-config-spruce": "^11.2.26",
    "jest": "^29.7.0",
    "jest-circus": "^29.7.0",
    "jsdom": "^27.0.0",
    "prettier": "^3.3.3",
    "ts-node": "^10.9.2",
    "tsc-watch": "^6.2.0",
    "typescript": "^5.6.3"
  },
  "jest": {
    "testRunner": "jest-circus/runner",
    "testEnvironment": "node",
    "testTimeout": 120000,
    "maxWorkers": 4,
    "testMatch": [
      "**/__tests__/**/*.test.js?(x)"
    ],
    "testPathIgnorePatterns": [
      "<rootDir>/tmp/",
      "<rootDir>/src/",
      "<rootDir>/node_modules/",
      "<rootDir>/build/__tests__/testDirsAndFiles/"
    ],
    "moduleNameMapper": {
      "^#spruce/(.*)$": "<rootDir>/build/.spruce/$1"
    },
    "setupFiles": [
      "<rootDir>/build/__tests__/setupTests.js"
    ]
  },
  "skill": {
    "namespace": "node-biosensors"
  }
}
